{
  "name": "yacinedev-ai-worker",
  "version": "1.0.0",
  "description": "YACINEDEV background AI worker — runs Kimi conversation off the shared PHP host so it survives visitor disconnects",
  "main": "server.js",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
